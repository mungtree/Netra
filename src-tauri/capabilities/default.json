{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window of NETRA.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
